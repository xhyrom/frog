import "frog::math";

pub fun ahoj(name) {
    return "ahooojky " + name;
}

pub fun fibo(n) {
    let phi = (1.0 + math.sqrt(5))/2.0;
    let asymp = math.pow(phi, n) / math.sqrt(5);

    return math.floor(asymp);
}